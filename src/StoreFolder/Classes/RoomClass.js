class Room {
    constructor(inventory, paths, description) {
      this.inventory = inventory;
      this.paths = paths;
      this.description = description;
    }
  }

export const RoomClass = Room